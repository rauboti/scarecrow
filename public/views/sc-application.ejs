<!DOCTYPE html>
<html>
  <head>
    <title><%=title%></title>
    <!-- Cleaner font -->
    <link href="https://fonts.googleapis.com/css?family=Rajdhani" rel="stylesheet">
    <!-- CSS source files -->
    <link rel="stylesheet" href="/css/boti.css" />
    <link rel="stylesheet" href="/css/scarecrow.css" />
    <nav class="nav-sc">
      <menu class="menu-sc">
        <a href="<%=scMenu['Home'].path%>"><div class="menu-button"><div class="menu-button-icon icon-home"></div></div></a>
        <% for (var i in scMenu) {
          if (scMenu[i].menu === 1) {
            if (i == activePage) {%>
            <a href="<%=scMenu[i].path%>"><div class="menu-button menu-button-selected"><div class="menu-button-text"><%=i%></div></div></a>
          <%} else {%>
            <a href="<%=scMenu[i].path%>"><div class="menu-button"><div class="menu-button-text"><%=i%></div></div></a>
        <%}}}%>
      </menu>
    </nav>
  </head>
  <body>
    <section id="pageContainer">
      <div class="container-sc">
        <div class="container-frame">
          <div class="container-body">
            <div class="container-frame"><%=application[0].character_name%> - Level <%=application[0].character_level%> <%=application[0].character_class%> (<%=application[0].character_role%>)</div>
            <table>
              <tr>
                <th># Spec</th>
              </tr>
              <tr>
                <td><a href="<%=application[0].spec%>" target="_blank"><u><%=application[0].spec%></u></a><br><br></td>
              </tr>
              <tr>
                <th># Armory profile</th>
              </tr>
              <tr>
                <td><a href="<%=application[0].armory%>" target="_blank"><u><%=application[0].armory%></u></a><br><br></td>
              </tr>
              <tr>
                <th># Raids / week</th>
              </tr>
              <tr>
                <td><div style="white-space: pre-wrap;"><%=application[0].raids%></div><br><br></td>
              </tr>
              <tr>
                <th># Raid preparations</th>
              </tr>
              <tr>
                <td><div style="white-space: pre-wrap;"><%=application[0].preparation%></div><br><br></td>
              </tr>
              <tr>
                <th># Why the applicant is a valuable asset</th>
              </tr>
              <tr>
                <td><div style="white-space: pre-wrap;"><%=application[0].asset%></div><br><br></td>
              </tr>
              <tr>
                <th># Response to being called out when making mistakes</th>
              </tr>
              <tr>
                <td><div style="white-space: pre-wrap;"><%=application[0].mistakes%></div><br><br></td>
              </tr>
              <tr>
                <th># Anything else?</th>
              </tr>
              <tr>
                <td><div style="white-space: pre-wrap;"><%=application[0].anything_else%></div></td>
              </tr>
            </table>
          </div>
          <div class="container-foot">
            <form name="frmApplication" method="post">
              <div class="buttonRow">
                <button type="submit" name="back" value="back" class="response-button icon-back"></button>
                <% if (application[0].status === 'New' || application[0].status === 'Declined') {%>
                  <button type="submit" name="accept" value="accept" class="response-button icon-accept"></button>
                <%}
                if (application[0].status === 'New' || application[0].status === 'Accepted') {%>
                <button type="submit" name="decline" value="decline" class="response-button icon-decline"></button>
                <%}%>
              </div>
            <form>
          </div>
        </div>
      </div>
    </section>
  </body>
  <foot>
    <!-- JS source files -->
    <script src="/js/jquery-3.3.1.min.js"></script>
    <script src="/js/scarecrow.js"></script>
    <script src="/js/sc-main.js"></script>
  </foot>
</html>
