<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="google" content="notranslate">
    <title><%=conf.title%></title>
    <!-- Cleaner font -->
    <link href="https://fonts.googleapis.com/css?family=Rajdhani" rel="stylesheet">
    <!-- CSS source files -->
    <% if (conf.theme === 'scarecrow') {%> <link rel="stylesheet" href="/css/sc-theme-scarecrow.css" />
    <%} else if (conf.theme === 'duel') {%> <link rel="stylesheet" href="/css/sc-theme-duel.css" />
    <%} else if (conf.theme === 'infernal') {%> <link rel="stylesheet" href="/css/sc-theme-infernal.css" />
    <%} else if (conf.theme === 'warzone') {%> <link rel="stylesheet" href="/css/sc-theme-warzone.css" />
    <%}%>
    <% if (conf.device === 'phone') {%> <link rel="stylesheet" href="/css/sc-mobile.css" />
    <%} else if (conf.device === 'desktop') {%> <link rel="stylesheet" href="/css/sc-desktop.css" />
    <%}%>
    <nav>
      <menu>
        <a href="<%=scMenu['home'].path%>"><div class="menuButtonRow-icon icon-logo"></div></a>
        <% if (conf.device === 'phone') {%>
        <div class="menuButtonRow-currentPage"><%=conf.page%></div>
        <div class="menuButtonRow-icon icon-menu"></div>
        <%}%>
        <div class="menuButtonRow">
          <% for (var i in scMenu) {
            if (scMenu[i].menu === 1) {
              if (scMenu[i].text == conf.page) {%>
              <a href="<%=scMenu[i].path%>"><div class="menuButton selected"><%=scMenu[i].text%></div></a>
            <%} else {%>
              <a href="<%=scMenu[i].path%>"><div class="menuButton"><%=scMenu[i].text%></div></a>
          <%}}}%>
        </div>
      </menu>
    </nav>
  </head>
  <body>
    <section class="pageContainer">
      <div class="appHeadline"><%=application[0].character_name%> - Level <%=application[0].character_level%> <%=application[0].character_class%> (<%=application[0].character_role%>)</div>
      <div class="appBody">
        <div><b># Spec</b></div>
        <div><a href="<%=application[0].spec%>" target="_blank"><u><%=application[0].spec%></u></a><br><br></div>
        <div><b># Armory profile</b></div>
        <div><a href="<%=application[0].armory%>" target="_blank"><u><%=application[0].armory%></u></a><br><br></div>
        <div><b># Raids / week</b></div>
        <div style="white-space: pre-wrap;"><%=application[0].raids%></div><br><br>
        <div><b># Raid preparations</b></div>
        <div style="white-space: pre-wrap;"><%=application[0].preparation%></div><br><br>
        <div><b># Why the applicant is a valuable asset</b></div>
        <div style="white-space: pre-wrap;"><%=application[0].asset%></div><br><br>
        <div><b># Being called out when making mistakes</b></div>
        <div style="white-space: pre-wrap;"><%=application[0].mistakes%></div><br><br>
        <div><b># Anything else?</b></div>
        <div style="white-space: pre-wrap;"><%=application[0].anything_else%></div>
      </div>
      <div class="appButtonRow">
        <form name="frmApplication" method="post">
          <button type="submit" name="back" value="back" class="action-button">< Back</button>
          <% if (application[0].status === 'New' || application[0].status === 'Declined') {%>
            <button type="submit" name="accept" value="accept" class="action-button">Accept</button>
          <%}
          if (application[0].status === 'New' || application[0].status === 'Accepted') {%>
            <button type="submit" name="decline" value="decline" class="action-button">Decline</button>
          <%}%>
        </form>
      </div>
    </section>
  </body>
  <foot>
    <!-- JS source files -->
    <script src="/js/jquery-3.3.1.min.js"></script>
    <script src="/js/scarecrow.js"></script>
    <script src="/js/application.js"></script>
    <% if (conf.device === 'phone') {%> <script>$('.menuButtonRow').hide()</script> <%}%>
  </foot>
</html>
