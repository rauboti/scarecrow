<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="google" content="notranslate">
    <title><%=conf.title%></title>
    <!-- Cleaner font -->
    <link href="https://fonts.googleapis.com/css?family=Rajdhani" rel="stylesheet">
    <!-- CSS source files -->
    <link rel="stylesheet" href="/css/boti-main.css" />
    <link rel="stylesheet" href="/css/boti-icons.css" />
    <% if (conf.theme === 'ghostly') {%>
      <link rel="stylesheet" href="/css/boti-theme-ghostly.css" />
    <%} else if (conf.theme === 'crimson') {%>
      <link rel="stylesheet" href="/css/boti-theme-crimson.css" />
    <%} else if (conf.theme === 'toxic') {%>
      <link rel="stylesheet" href="/css/boti-theme-toxic.css" />
    <%} else if (conf.theme === 'angelic') {%>
      <link rel="stylesheet" href="/css/boti-theme-angelic.css" />
    <%} else if (conf.theme === 'obsidian') {%>
      <link rel="stylesheet" href="/css/boti-theme-obsidian.css" />
    <%}%>
    <% if (conf.device === 'phone') {%>
      <link rel="stylesheet" href="/css/boti-mobile.css" />
    <%} else if (conf.device === 'desktop') {%>
      <link rel="stylesheet" href="/css/boti-desktop.css" />
    <%}%>
    <link rel="stylesheet" href="/css/boti-extras.css" />
    <nav class="pos-absolute">
      <menu class="menu-themed">
        <a href="<%=scMenu['home'].path%>"><div class="menu-button is-inline"><div class="menu-button-icon icon-logo"></div></div></a>
        <div class="show-mobile col-50 is-inline selected-page"><%=conf.page%></div>
        <div class="menu-button is-inline show-mobile"><div class="menu-button-icon icon-menu"></div></div>
        <div class="menu-button-row show-desktop">
          <% for (var i in scMenu) {
            if (scMenu[i].menu === 1) {
              if (scMenu[i].text == conf.page) {%>
              <a href="<%=scMenu[i].path%>"><div class="menu-button button-selected-themed"><div class="menu-button-text text-selected"><%=scMenu[i].text%></div></div></a>
            <%} else {%>
              <a href="<%=scMenu[i].path%>"><div class="menu-button"><div class="menu-button-text"><%=scMenu[i].text%></div></div></a>
          <%}}}%>
        </div>
      </menu>
    </nav>
  </head>
  <body>
    <section>
      <div class="container-page align-center is-scrollable">
        <div class="container-medium align-center frame-themed background-themed"><%=application[0].character_name%> - Level <%=application[0].character_level%> <%=application[0].character_class%> (<%=application[0].character_role%>)</div>
        <div class="container-medium align-left frame-themed background-themed">
          <div><b># Spec</b></div>
          <div><a href="<%=application[0].spec%>" target="_blank"><u><%=application[0].spec%></u></a><br><br></div>
          <div><b># Armory profile</b></div>
          <div><a href="<%=application[0].armory%>" target="_blank"><u><%=application[0].armory%></u></a><br><br></div>
          <div><b># Raids / week</b></div>
          <div style="white-space: pre-wrap;"><%=application[0].raids%></div><br><br>
          <div><b># Raid preparations</b></div>
          <div style="white-space: pre-wrap;"><%=application[0].preparation%></div><br><br>
          <div><b># Why the applicant is a valuable asset</b></div>
          <div style="white-space: pre-wrap;"><%=application[0].asset%></div><br><br>
          <div><b># Being called out when making mistakes</b></div>
          <div style="white-space: pre-wrap;"><%=application[0].mistakes%></div><br><br>
          <div><b># Anything else?</b></div>
          <div style="white-space: pre-wrap;"><%=application[0].anything_else%></div>
        </div>
        <div class="container-medium align-center frame-themed background-themed">
          <form name="frmApplication" method="post">
            <button type="submit" name="back" value="back" class="button-icon-medium icon-back"></button>
            <% if (application[0].status === 'New' || application[0].status === 'Declined') {%>
              <button type="submit" name="accept" value="accept" class="button-icon-medium icon-accept"></button>
            <%}
            if (application[0].status === 'New' || application[0].status === 'Accepted') {%>
              <button type="submit" name="decline" value="decline" class="button-icon-medium icon-decline"></button>
            <%}%>
          </form>
        </div>
      </div>
    </section>
  </body>
  <foot>
    <!-- JS source files -->
    <script src="/js/jquery-3.3.1.min.js"></script>
    <script src="/js/scarecrow.js"></script>
    <script src="/js/mobile.js"></script>
    <script src="/js/main.js"></script>
  </foot>
</html>
